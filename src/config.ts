/** App-wide constants shared across screens (title, settings, store metadata). */

/** User-facing version, shown in Settings. Keep in step with package.json /
 *  the native MARKETING_VERSION. */
export const APP_VERSION = '1.0';

/** Hosted privacy policy (no data collected). Same URL used for the store listings. */
export const PRIVACY_URL = 'https://github.com/QuinnPorter/WestminsterSim/blob/main/PRIVACY.md';
